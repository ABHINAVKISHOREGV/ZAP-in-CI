version: 2.1
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: docker run -it owasp/zap2docker-weekly zap.sh -cmd -quickurl https://staging.rently.com -quickprogress -quickout /tmp/testreport.xml
#       - run: cat /tmp/testreport.xml > /tmp/testreport.txt
      - store_artifacts:
         path: /tmp/testreport.xml
