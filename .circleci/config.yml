version: 2.1
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: docker run -it owasp/zap2docker-weekly zap.sh -cmd -quickurl https://example.com -quickout /tmp/testreport.xml
      - run: ls /tmp/
      - store_artifacts:
         path: /tmp/testreport.xml
